#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
* main - print alphabet
* Return: always 0
*/
int main(void)
{
	char c;

	c = 'a';
	while
		(c <= 'z') {
		c++;
	if (c != 'q' && c != 'e')
	putchar(c);
	c++;
	}
putchar('\n');
return (0);
}
