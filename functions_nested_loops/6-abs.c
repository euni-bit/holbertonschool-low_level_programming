#include "main.h"
#include <stdio.h>
/**
 * _abs - compute the absolute value if integer
 * @n: the integer to be computed.
 *
 * Return: the absolute value of the interger
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
	return (0);
}
