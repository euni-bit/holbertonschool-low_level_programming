#include "main.h"
#include <stdio.h>
/**
 * mul - mutiplies two integers
 * @a: first operand
 * @b: second operand
 *
 * Return: Product of both operands
 */
int mul(int a, int b)
{
	return (a * b);
}
